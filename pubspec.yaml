name: google_signin_ii_example
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.3

  # Google Sign In
  firebase_auth: ^1.1.4
  google_sign_in: ^5.0.3

  # Google Icon
  font_awesome_flutter: ^9.0.0

  # State Management
  provider: ^5.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true